"""Legacy placeholder retained for compatibility.

Data generation is now part of the package pipeline:
`python -m transformer_mppi.cli reproduce --task <navigation2d|racing|both> --profile quick`
"""
